import { Button } from "@material-ui/core";
import Layout from '../../../components/Layout/Layout'

export default function ConfirmAccount(){
    return(
        <Layout>
        <h2>colMotors</h2>
        <Button variant="contained" color='secondary'>Confirmar Cuenta</Button>
        </Layout>
    )
}